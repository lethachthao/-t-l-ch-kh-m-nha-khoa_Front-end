// em lưu ý: từ thằng app folder vào trong, thì tất cả các page bên trong là một router, nó có thể truy cập được từ URL
// ở đây admin là folder, nên URL sẽ là localhost:3000/admin
// (home) là một cái group folder mình đặt để cho nó có nghĩa thôi, để biết tất cả các page nằm bên trong (home) thuộc về trang chủ để tránh lộn xộn

export default function AdminPage() {
  return <div>tui la admin</div>;
}
